@model ECommerceWebsite.Models.Product_Categories

<h3>@Resources.Resource.DeleteQuestion</h3>
<hr />
<div>
    <dl class="dl-horizontal">
        <dt>
            <label for="CategoryTitle" class="control-label col-md-2">@Resources.Resource.CategoryTitle</label>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CategoryTitle)
        </dd>

        @*<dt>
            @Html.DisplayNameFor(model => model.Product_Categories2.CategoryTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Product_Categories2.CategoryTitle)
        </dd>*@

    </dl>

    @using (Ajax.BeginForm("Delete", "Product_Categories", FormMethod.Post,
    new AjaxOptions()
    {
        UpdateTargetId = "tree1",
        OnSuccess = "closeModal"
    }))
    {
        @Html.AntiForgeryToken()

    <div class="modal-footer text-center">
        <button type="submit" class="btn btn-default" data-dismiss="modal">
            @Resources.Resource.Cancel
        </button>
        <button type="submit" class="btn btn-success">
            @Resources.Resource.OK
        </button>
    </div>
    }
</div>
